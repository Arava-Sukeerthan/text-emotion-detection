[build]
  base = "frontend"
  publish = "."
  command = "echo 'Static build complete'"

[build.environment]
  PIP_REQUIREMENTS_FILE = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
